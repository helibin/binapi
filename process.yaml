apps:
  - script: './src/app.js'
    name: 'BinAPI'
    instances: 'max'
    exec_mode: cluster
    watch: false
    merge_logs: true
    max_restarts: 5
    restart_delay: 1000
    env:
      NODE_ENV: 'production'
